<Window x:Class="Notepad.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        mc:Ignorable="d"
        Title="Notepad++"
        Height="450"
        Width="800">

    <Grid>
        <DockPanel>

            <Menu DockPanel.Dock="Top">

                <MenuItem Header="File">
                    <MenuItem Header="New file" Command="{Binding NewFileCommand}" />
                    <MenuItem Header="Open file..." Command="{Binding OpenFileCommand}" />
                    <MenuItem Header="Save file" Command="{Binding SaveFileCommand}" />
                    <MenuItem Header="Save file as..." Command="{Binding SaveFileAsCommand}" />
                    <Separator />
                    <MenuItem Header="Close all files" Command="{Binding CloseAllFilesCommand}" />
                    <MenuItem Header="Exit" Command="{Binding ExitCommand}" />
                </MenuItem>

                <MenuItem Header="Edit">
                    <MenuItem Header="Copy" Command="{Binding CopyCommand}" />
                    <MenuItem Header="Cut" Command="{Binding CutCommand}" />
                    <MenuItem Header="Paste" Command="{Binding PasteCommand}" />
                    <Separator />
                    <MenuItem Header="UPPERCASE" Command="{Binding ToUpperCaseCommand}" />
                    <MenuItem Header="lowercase" Command="{Binding ToLowerCaseCommand}" />
                    <Separator />
                    <MenuItem Header="Go to line..." Command="{Binding GoToLineCommand}" />
                    <MenuItem Header="Remove empty lines" Command="{Binding RemoveEmptyLinesCommand}" />
                    <Separator />
                    <MenuItem Header="Set/Unset Read Only" Command="{Binding ToggleReadOnlyCommand}" />
                </MenuItem>

                <MenuItem Header="Search">
                    <MenuItem Header="Find" Command="{Binding FindCommand}" />
                    <MenuItem Header="Replace" Command="{Binding ReplaceCommand}" />
                </MenuItem>

                <MenuItem Header="View">
                    <MenuItem Header="Standard" Command="{Binding ShowStandardViewCommand}" />
                    <MenuItem Header="Folder Explorer" Command="{Binding ShowFolderExplorerCommand}" />
                </MenuItem>

                <MenuItem Header="Help">
                    <MenuItem Header="About" Click="AboutMenuItem_Click" />
                </MenuItem>

            </Menu>

            <ToolBar DockPanel.Dock="Top">

                <Button Command="{Binding NewFileCommand}" ToolTip="New file">
                    <svgc:SvgViewbox Source="/Resources/Icons/new-file.svg" Width="15" Height="15" />
                </Button>
                <Button Command="{Binding OpenFileCommand}" ToolTip="Open file">
                    <svgc:SvgViewbox Source="/Resources/Icons/open.svg" Width="15" Height="15" />
                </Button>
                <Button Command="{Binding SaveFileCommand}" ToolTip="Save file">
                    <svgc:SvgViewbox Source="/Resources/Icons/save.svg" Width="15" Height="15" />
                </Button>
                <Button Command="{Binding SaveFileAsCommand}" ToolTip="Save file as...">
                    <svgc:SvgViewbox Source="/Resources/Icons/save-as.svg" Width="15" Height="15" />
                </Button>
                <Button Command="{Binding CloseAllFilesCommand}" ToolTip="Close all files">
                    <svgc:SvgViewbox Source="/Resources/Icons/close-all-files.svg" Width="15" Height="15" />
                </Button>
                <Button Command="{Binding ExitCommand}" ToolTip="Exit">
                    <svgc:SvgViewbox Source="/Resources/Icons/exit.svg" Width="15" Height="15" />
                </Button>

                <Separator />

                <Button Command="{Binding FindCommand}" ToolTip="Find">
                    <svgc:SvgViewbox Source="/Resources/Icons/search.svg" Width="15" Height="15" />
                </Button>
                <Button Command="{Binding ReplaceCommand}" ToolTip="Replace">
                    <svgc:SvgViewbox Source="/Resources/Icons/replace.svg" Width="15" Height="15" />
                </Button>

            </ToolBar>

            <TreeView x:Name="MyTreeView"
                      Tag="{Binding}"
                      DockPanel.Dock="Left"
                      Width="250"
                      ItemsSource="{Binding FolderTree.Partitions}"
                      MouseDoubleClick="TreeView_MouseDoubleClick">

                <TreeView.Style>
                    <Style TargetType="TreeView">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowFolderExplorer}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.Style>

                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">

                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />

                        <Setter Property="Tag"
                                Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Window}}" />

                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="New file"
                                              Command="{Binding PlacementTarget.Tag.NewFileInFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="Copy path"
                                              Command="{Binding PlacementTarget.Tag.CopyPathCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="Copy folder"
                                              Command="{Binding PlacementTarget.Tag.CopyFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="Paste folder"
                                              Command="{Binding PlacementTarget.Tag.PasteFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>

                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                        <StackPanel Orientation="Horizontal">

                            <svgc:SvgViewbox Source="/Resources/Icons/folder.svg"
                                             Width="16"
                                             Height="16"
                                             Margin="0,0,5,0">
                                <svgc:SvgViewbox.Style>
                                    <Style TargetType="svgc:SvgViewbox">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </svgc:SvgViewbox.Style>
                            </svgc:SvgViewbox>

                            <svgc:SvgViewbox Source="/Resources/Icons/file.svg"
                                             Width="16"
                                             Height="16"
                                             Margin="0,0,5,0">
                                <svgc:SvgViewbox.Style>
                                    <Style TargetType="svgc:SvgViewbox">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </svgc:SvgViewbox.Style>
                            </svgc:SvgViewbox>

                            <TextBlock Text="{Binding Name}" />

                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>

            </TreeView>

            <TabControl ItemsSource="{Binding Tabs}"
                        SelectedItem="{Binding SelectedTab}">

                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">

                            <svgc:SvgViewbox Source="/Resources/Icons/unsaved.svg"
                                             Width="14"
                                             Height="14"
                                             Margin="0,0,5,0"
                                             VerticalAlignment="Center">
                                <svgc:SvgViewbox.Style>
                                    <Style TargetType="svgc:SvgViewbox">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSaved}" Value="False">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </svgc:SvgViewbox.Style>
                            </svgc:SvgViewbox>

                            <TextBlock Text="{Binding Header}"
                                       VerticalAlignment="Center" />

                            <TextBlock Text="×"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       Cursor="Hand"
                                       Foreground="Gray">
                                <TextBlock.InputBindings>
                                    <MouseBinding MouseAction="LeftClick"
                                                  Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                  CommandParameter="{Binding}" />
                                </TextBlock.InputBindings>
                            </TextBlock>

                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 AcceptsTab="True"
                                 IsReadOnly="{Binding IsReadOnly}"
                                 FontSize="14"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Disabled"
                                 IsInactiveSelectionHighlightEnabled="True"
                                 SelectionChanged="TextBox_SelectionChanged" />
                    </DataTemplate>
                </TabControl.ContentTemplate>

            </TabControl>

        </DockPanel>
    </Grid>

</Window>